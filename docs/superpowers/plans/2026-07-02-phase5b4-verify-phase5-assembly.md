# Phase 5B-4: verify-phase5 Assembly Implementation Plan (rev 2 — post adversarial review)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** The Phase-5 gate closes: an internal analyst agent answers `evals/phase5_questions.yaml` via text-to-SQL (≥90% correct, 100% of answered questions carry mechanically-resolving citations, REFUSE cases refused with the right reason class), `verify-phase5` assembles every phase gate into one verdict, and the launch checklist is executable. ANTHROPIC_API_KEY absent → live eval reports BLOCKED like the dossier gate; one key unblocks the remaining roadmap.

**Rev-2 deltas (all 14 review findings bound in):** DuckDB native sandbox (`SET enable_external_access = false`) replaces keyword-denylist security; citations.parquet gains pe_bli/scenario/amount_type for pdf_page resolution; REFUSE enum + `expected_refuse_class` yaml field; per-table URL-column map; eval canonicalization extended to q019/q025/q030/q033/q034/q040; citation re-execution through a fresh sql_tool (same sandbox + cwd); recompute must match EXPECTED (not just the agent's echo) for scored-correct answers, non-empty touched tables ∩ answer_sql tables; verdict-token regex parsing (never last-line); exit 2 = all-green-but-BLOCKED-on-key vs exit 1 = FAIL; prompt caching + cost note; CORS `MaxAgeSeconds` + Origin-header assertions; 4th stale count (downloads/page.tsx:71) + stale-literal grep in launch_gate; per-dataset counts single-sourced; require_client extracted with message param; NEW FakeClient for messages.create tool loops; task splits 2a/2b + 3a/3b.

**Recon facts (verified live 2026-07-01/02; do NOT rediscover):**
- Eval drift: q009 →293.145, q013 →16, q022, q031, q032 →490, q034 (CT ~2×), q035 (CA ~12.7×), q038 →3.79, q040 →2017-2026 (all re-verified). q045's REFUSE is FALSE (FY2024: 4.42M transactions, $445.95B ingested) → rewrite to FY2027 REFUSE. Stale notes q009/q040. 17 questions carry tolerance.
- **Answer-shape traps:** q019's answer_sql SELECTs 3 cols but expected has 2 (refresh would corrupt it — trim the SELECT to (pe_bli, program_title) ordered by count desc); q025/q030/q033/q034 expected answers contain values their questions never ask (trim SELECT lists in the same pass); q040 answer_sql rewritten to return one formatted string.
- **REFUSE enum (binding, three places):** `{data_not_ingested, structurally_absent, classified}`. q041 data_not_ingested; q042 structurally_absent; q043 classified; q044 data_not_ingested; q045-new data_not_ingested.
- **Sandbox reality (verified live):** on a read-only DuckDB connection, `select content from read_text('evals/phase5_questions.yaml')` SUCCEEDS — read_csv/read_parquet/read_json/glob/getenv are all legal inside SELECT. The ONLY robust fix: materialize oversight parquets as TEMP TABLES first, then `SET enable_external_access = false` (blocks all file/network functions), keep the statement gate (single statement, WITH/SELECT start, deny ATTACH/COPY/INSTALL/LOAD/PRAGMA/SET) as belt-and-braces — the agent must not be able to un-set the sandbox: verify SET is denied by the gate.
- **citations.parquet has NO pe_bli column** (fact_id, kind, units, amount_text, page_number, …). export_site.py section 4a already SELECTs p.pe_bli and drops it. 46% of jbook amount_text values (963/2,102) repeat across rows — amount-only matching proves nothing. amount_text is comma-grouped 3-decimals: canonical formatter `f"{millions:,.3f}"`.
- **URL-column map (binding):** improper-payment questions resolve source_url from the `improper_payments` temp table (fct_improper_exposure has NO url column); state spend → fct_state_per_capita.spend_source_url; state population → .pop_source_url; GAO → `high_risk`.source_url.
- verify_phase5b3.py's BLOCKED branch prints a 2-line block ending "To unblock: …" — the LAST line is NOT the verdict. Parse full output with `re.search(r"verify-phase\w+:\s*(PASS|FAIL|BLOCKED)")` + returncode.
- require_client at dossiers/batch.py:441-449, referenced by submit() :505 and collect() :669; _NO_KEY_MESSAGE :428-438 is dossier-specific → extracted fn takes a message param.
- Existing FakeClient (tests/test_dossiers_batch.py:227) fakes batches/count_tokens only — the analyst needs a NEW FakeClient whose messages.create pops scripted responses (tool_use blocks), with the loop implemented manually (while + tool_choice-forced submit_answer final turn).
- Cost: ~45 questions × ~8-turn sonnet-4-6 loop ≈ 3.6M input/180K output uncached → add `cache_control` on the schema-card system block (stable prefix, no timestamps), print a dossier-style cost estimate when live.
- Stale counts: download-cards.tsx:92,166 + prepare-assets.mjs:184 (llms.txt) + **downloads/page.tsx:71** + build.mjs:13 comment. Per-dataset row counts live in data/site/manifest.json `datasets` (site_meta.json counts has only totals) — extend export_site to copy the datasets dict into site_meta.json so the site build has ONE source.
- Broken href: download-cards.tsx `/data/citations.parquet` — file lives at `/citations/citations.parquet`. Fix href; gate server + R2 layout unchanged.
- CORS: field is `MaxAgeSeconds` (not MaxAge); R2 sends ACAO only when Origin matches — cors_live_test.sh must send `-H "Origin: $SITE_URL"` on BOTH the OPTIONS preflight AND the ranged GET, asserting ACAO echo each time, Allow-Headers contains range on OPTIONS, 206+Content-Range+Accept-Ranges on GET.
- Launch facts: runtime R2 set = pdfs/149M + data/3.2M + workbooks/1.2M + citations/1M; config.json rewrite mechanism greenfield; root vercel.json untouchable; site/out 1.0GB/49k files (Vercel-limits check is a LAUNCH.md item); SITE_URL placeholder single-sourced from NEXT_PUBLIC_SITE_URL; ROADMAP uncited_datasets wording 2→actual 4 (dim_geography, dim_lobbyists, fct_budget_to_awards, jbook_narratives).
- Units/additivity traps + REFUSE knowledge for the schema card: per the 5B-4 recon (thousands vs millions vs USD; family_obligations_usd NON-ADDITIVE; feed units column; district high-confidence-only; negative obligations; derived_improper labeled; dim_entities FY17-23 window vs transactions FY17-26; FY2026-edition-only budget lines; no bid data; classified placeholder 9999999999; CA/CT only; no FY2027).

**Evaluator design:** as rev 1, hardened: `eval_freshness_gate` (re-run answer_sql, fail loud on drift); `eval_gate` — scoring: accuracy ≥41/45 (tolerance-aware, canonical string compare); REFUSE = agent.refuse AND exact enum match on expected_refuse_class; citation resolution over ANSWERED questions only: re-execute agent.sql through a FRESH sql_tool (sandboxed, cwd-correct) → non-empty touched-tables required, touched ∩ tables(answer_sql) non-empty, recompute matches agent.answer AND (when the answer scored correct) the EXPECTED answer within tolerance; pdf_page → citations.parquet row on (pe_bli, canonical amount_text) with page_number; source_url → per-table URL-column map. 100% required. BLOCKED (not FAIL) without the key. `assembly_gate` — verdict-regex over full child output; exit 0 all-PASS, exit 2 all-green-but-BLOCKED-on-key, exit 1 any FAIL. `launch_gate` — download hrefs resolve via gate server, built output contains NO stale "44,754" literal, counts data-driven, config-rewrite round-trips, cors_live_test self-skips loudly. Proof-it-can-fail tests for EVERY gate: read_text rejected by sandbox; literal-echo `select 42` citation unresolvable; right-amount-wrong-pe_bli pdf citation unresolvable; population-answer-citing-spend_source_url fails; refuse-right-answer-wrong-class fails; tampered expected_answer caught by freshness; BLOCKED-output parsing unit test.

---

### Task 1: Eval refresh tooling + corrected set
**Files:** src/govbudget/evals_refresh.py, CLI `evals refresh|check`, evals/phase5_questions.yaml, tests/test_eval_set.py.
As rev 1 PLUS: canonicalize q019 (SELECT (pe_bli, program_title) order by count(distinct award_piid) desc nulls last — verified returns the same row), trim q025/q030/q033/q034 SELECT lists to exactly what each question asks, rewrite q040's sql to return one formatted string; add `expected_refuse_class` (enum above) to all 5 REFUSE entries incl. rewritten q045; test_eval_set.py: REFUSE entries must carry expected_refuse_class ∈ enum. Refresh runs with cwd=config.ROOT. Idempotency + tamper-detection tests. Commit.

### Task 2a: common.anthropic_client extraction
**Files:** src/govbudget/common/{__init__,anthropic_client}.py, dossiers/batch.py, tests.
`require_client(client=None, *, message)` — message param (dossier text default preserved verbatim for batch.py callers; every variant contains 'ANTHROPIC_API_KEY' and 'export ANTHROPIC_API_KEY'). batch.py imports it; ALL existing dossier tests pass unchanged. Commit.

### Task 2b: Analyst agent
**Files:** src/govbudget/analyst/{__init__,agent,schema_card,sql_tool}.py, CLI `analyst`, tests/analyst/ (NEW FakeClient).
- sql_tool: connect read_only → CREATE TEMP TABLE improper_payments/high_risk from ABSOLUTE parquet paths → `SET enable_external_access = false` → statement gate (single statement; WITH/SELECT start; deny ATTACH/COPY/INSTALL/LOAD/PRAGMA/**SET**) → 200-row/30s caps. Returns rows + touched tables (EXPLAIN-based or conservative regex over identifiers ∩ known tables). Tests: read_text/read_parquet/glob/getenv all REJECTED at execution (sandbox) AND SET rejected at the gate; DML rejected; happy SELECT ok.
- schema_card: checked-in structured dict (tables/columns/units/traps/REFUSE guidance incl. the enum + URL-column map guidance) → rendered system prompt with `cache_control: {type:'ephemeral'}` on the stable block; drift test vs live information_schema.
- agent: manual tool loop (messages.create, while tool_use, execute run_sql, append tool_result; final turn tool_choice-forces submit_answer {answer, refuse, refuse_reason_class: enum|null, sql, citation_kind, citation}); max 8 turns; cost accumulator printed. CLI `govbudget analyst "..."` (require_client with analyst message). FakeClient: messages.create pops scripted responses; tests: numeric happy path, refusal, tool-loop termination, forced final. Commit.

### Task 3a: eval_gate scoring + citation resolution
**Files:** src/govbudget/verify_phase5.py (scoring half), tests/test_verify_phase5.py.
Scoring per the evaluator design above (all rules + the denominator sentence: resolution is evaluated over agent-answered questions only; correctness is scored solely by accuracy — a wrong answer with a self-consistent citation counts against accuracy, not resolution). Citation re-execution through a fresh sql_tool instance. pdf_page resolver reads citations.parquet (pe_bli + canonical amount_text + page_number non-null) — REQUIRES Task 4's export change; source_url resolver uses the URL-column map. Full mocked test matrix incl. every proof-it-can-fail case listed in the evaluator design. Commit.

### Task 3b: verify-phase5 assembly + CLI
**Files:** verify_phase5.py (assembly half), CLI wiring, tests.
Freshness gate first; eval_gate (BLOCKED path); assembly: subprocess verify-phase{1,2,3,4,5a,5b1,5b3} (NOT 5b2 — 5b3 runs the same npm suite), verdict-regex over FULL output + returncode; result table; exit 0/2/1 per the design (2 = everything green except key-BLOCKED items, printing the single unblock step). Unit test parses a captured verify-phase5b3 BLOCKED transcript correctly. Commit.

### Task 4: Launch fixes on the site + citations export extension
**Files:** src/govbudget/export_site.py (+pe_bli/scenario/amount_type in citations.parquet section 4a; copy manifest datasets dict into site_meta.json counts), tests/jbooks/test_export_site_pg.py, site/src/components/download-cards.tsx (href + counts), site/src/app/downloads/page.tsx (:71 count), site/scripts/prepare-assets.mjs (llms.txt counts), site/scripts/gates/build.mjs (:13 comment; +download-href 200 checks; +stale-literal grep: built downloads page must NOT contain "44,754" unless manifest says so), docs/superpowers/ROADMAP.md (uncited_datasets 2→4 wording).
Then LIVE: `export-site` (citations.parquet now carries pe_bli) → verify-phase5b1 PASS → site rebuild → npm verify PASS. Commit.

### Task 5: Launch tooling + checklist
As rev 1 with corrections: cors-policy.json uses `MaxAgeSeconds: 3600`; cors_live_test.sh sends Origin on OPTIONS + GET and asserts ACAO echo both times, Allow-Headers∋range, 206/Content-Range/Accept-Ranges; upload_r2.sh (rclone, dry-run default) syncs pdfs/ data/ workbooks/ citations/; rewrite-config.mjs; LAUNCH.md ordered checklist (incl. Vercel free-tier limits check vs out/ 1.0GB/49k files with filing/ mitigation options; NEXT_PUBLIC_SITE_URL via *.vercel.app while domain TABLED; the API-key pair: dossiers submit + verify-phase5 live). Commit.

### Task 6: THE LOOP + final review + merge + roadmap close
- Loop: `evals refresh` (commit yaml) → `verify-phase5` → expected terminal state: freshness PASS, eval BLOCKED, assembly all-PASS + dossier BLOCKED, launch_gate PASS, **exit 2**. Fix until exactly that.
- Regression: pytest + vitest + npm verify + verify-phase1..5b3.
- Final opus review (sandbox escapes, scoring gameability residual, injection surface, launch script safety) → fix → merge + push.
- ROADMAP 5B-4 ✅ + roadmap-complete summary; memory update; user report (single unblock: export ANTHROPIC_API_KEY → `govbudget dossiers submit` + `govbudget verify-phase5`; LAUNCH.md; domain tabled).

---
## Self-Review Notes (rev 2)
- Every review finding has a home; the sandbox now relies on DuckDB's enable_external_access (verified mechanism) rather than string matching; resolution can no longer be satisfied by echo (non-empty tables ∩ answer_sql tables + expected-match for correct answers) — residual accepted and documented: a semantically-wrong-but-value-equal sql over an overlapping table.
- Exit-code contract: 0 ship / 2 needs-key / 1 broken — machine-distinguishable, precedent-compatible.
- Proof-it-can-fail tests enumerated per gate (standing rule).
